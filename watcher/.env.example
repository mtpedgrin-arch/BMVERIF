# ── Base de datos (misma que Vercel) ──────────────────────────────────────────
DATABASE_URL=postgresql://...

# ── npprteam.shop ─────────────────────────────────────────────────────────────
NPPRTEAM_API_KEY=tu_api_key_de_npprteam
NPPRTEAM_EMAIL=tu_email_de_npprteam
NPPRTEAM_PASSWORD=tu_password_de_npprteam

# ── Cryptomus (mismos que Vercel) ────────────────────────────────────────────
CRYPTOMUS_MERCHANT_UUID=tu_merchant_uuid
CRYPTOMUS_API_KEY=tu_api_key

# ── Telegram (mismos que Vercel) ─────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=tu_bot_token
TELEGRAM_ORDERS_CHAT_ID=tu_chat_id_de_ordenes

# ── Email (Resend API key — misma que Vercel SMTP_PASS) ──────────────────────
SMTP_PASS=re_...
SMTP_FROM=soporte@mail.bmverificada.space

# ── Configuración del watcher ────────────────────────────────────────────────
# Extra $ a depositar sobre el costo exacto (margen de seguridad)
DEPOSIT_BUFFER=10
